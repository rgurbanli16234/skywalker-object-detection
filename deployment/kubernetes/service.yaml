apiVersion: v1
kind: Service
metadata:
  name: skywalker-detector-service
  namespace: ml-production
  labels:
    app: skywalker-detector
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: skywalker-detector
